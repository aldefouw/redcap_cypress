version: 2.1
orbs:
  cypress: cypress-io/cypress@1
workflows:
  build:
    jobs:
      - cypress/install:
          pre-steps:
            - checkout
            - run:
                name: Build the REDCap image
                command: |
                    cd rdc
                    set -x
                    docker-compose up -d
      - cypress/run